export enum PaymentStatus {
    PENDING = 'pending',
    FAILED = 'failed',
    REFUNDED = 'refunded',
    INITIATED = 'initiated',
    SUCCESSFUL = 'successful',
    CANCELLED = 'cancelled',
    TIMEOUT = 'timeout',
}